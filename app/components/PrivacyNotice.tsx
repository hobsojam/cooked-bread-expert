export function PrivacyNotice() {
  return (
    <p className="privacy-note">
      Demo sessions expire after 6 hours. Use aliases and sample content only.
      Real sessions should be run on a self-hosted deployment with suitable data
      protection controls.
    </p>
  );
}
